import state from './state'
import { } from '../actionTypes'
let initState = state
const tag = (state = initState, { type, data }) => {
    switch (type) {
        default:
            return state
    }
}

export default tag